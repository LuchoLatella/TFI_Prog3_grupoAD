info:
  name: GET
  type: http
  seq: 3

http:
  method: GET
  url: GET http://localhost:3000/especialidades/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
